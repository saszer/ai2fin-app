# Wazuh Dashboard Configuration
# OpenSearch Dashboards configuration for Wazuh Dashboard
# Single-node setup for Fly.io
# embracingearth.space
#
# SIMPLIFIED ARCHITECTURE: Dashboard listens directly on port 5601
# No Nginx proxy - direct binding ensures Fly.io can detect the service

server.host: "::"
server.port: 5601  # Dashboard listens directly on 5601 (Fly.io internal_port)

# OpenSearch connection with authentication
opensearch.hosts: ["http://localhost:9200"]
opensearch.ssl.verificationMode: none

# Admin credentials for Dashboard-to-Indexer connection
# Password will be replaced by cont-init.d/10-set-indexer-dashboard-passwords.sh
# Default: admin (will be replaced with actual password from environment)
opensearch.username: "admin"
opensearch.password: "admin"

# Performance settings - increased timeouts for stability
opensearch.shardTimeout: 60000
opensearch.requestTimeout: 60000
opensearch.pingTimeout: 30000

# Request headers whitelist
opensearch.requestHeadersWhitelist: ["securitytenant","Authorization"]
